"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SettlementRepository = void 0;
const common_1 = require("@nestjs/common");
const mongoose_1 = require("@nestjs/mongoose");
const mongoose_2 = require("mongoose");
const settlement_schema_1 = require("../../../settlement/settlement.schema");
const settlement_mapper_1 = require("../mapper/settlement.mapper");
const settlement_error_1 = require("../../../settlement/settlement.error");
const group_schema_1 = require("../../../group/group.schema");
let SettlementRepository = class SettlementRepository {
    settlementModel;
    groupModel;
    constructor(settlementModel, groupModel) {
        this.settlementModel = settlementModel;
        this.groupModel = groupModel;
    }
    async create(settlement) {
        this.validateSettlement(settlement);
        const settlementData = settlement_mapper_1.SettlementMapper.toEntity(settlement);
        const createdSettlement = await this.settlementModel.create(settlementData);
        const populated = await this.settlementModel
            .findById(createdSettlement._id)
            .populate('fromUserId', 'name email')
            .populate('toUserId', 'name email')
            .exec();
        const domain = settlement_mapper_1.SettlementMapper.toDomain(populated);
        if (!domain) {
            throw new Error('Failed to create settlement domain');
        }
        return domain;
    }
    async findById(id) {
        const settlement = await this.settlementModel
            .findById(id)
            .populate('fromUserId', 'name email')
            .populate('toUserId', 'name email')
            .exec();
        if (!settlement) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
        return settlement_mapper_1.SettlementMapper.toDomain(settlement);
    }
    async findAll(groupId, filters) {
        const query = {
            groupId: new mongoose_2.Types.ObjectId(groupId),
        };
        if (filters?.status) {
            query.status = filters.status;
        }
        if (filters?.fromDate) {
            query.date = { $gte: filters.fromDate };
        }
        if (filters?.toDate) {
            query.date = { ...query.date, $lte: filters.toDate };
        }
        if (filters?.userId) {
            const userId = new mongoose_2.Types.ObjectId(filters.userId);
            query.$or = [
                { fromUserId: userId },
                { toUserId: userId },
            ];
        }
        if (filters?.isAutoGenerated !== undefined) {
            query.isAutoGenerated = filters.isAutoGenerated;
        }
        const settlements = await this.settlementModel
            .find(query)
            .populate('fromUserId', 'name email')
            .populate('toUserId', 'name email')
            .sort({ date: -1, createdAt: -1 })
            .exec();
        return settlements
            .map(settlement => settlement_mapper_1.SettlementMapper.toDomain(settlement))
            .filter(Boolean);
    }
    async update(id, settlement) {
        const existing = await this.findById(id);
        if (!existing) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
        this.validateSettlement(settlement);
        const updateData = settlement_mapper_1.SettlementMapper.toEntity(settlement);
        const updatedSettlement = await this.settlementModel
            .findByIdAndUpdate(id, updateData, { new: true })
            .populate('fromUserId', 'name email')
            .populate('toUserId', 'name email')
            .exec();
        if (!updatedSettlement) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
        const domain = settlement_mapper_1.SettlementMapper.toDomain(updatedSettlement);
        if (!domain) {
            throw new Error('Failed to update settlement domain');
        }
        return domain;
    }
    async updateStatus(id, status) {
        const settlement = await this.settlementModel.findById(id);
        if (!settlement) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
        this.validateStatusTransition(settlement.status, status);
        settlement.status = status;
        await settlement.save();
        const updatedSettlement = await this.settlementModel
            .findById(id)
            .populate('fromUserId', 'name email')
            .populate('toUserId', 'name email')
            .exec();
        if (!updatedSettlement) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
        const domain = settlement_mapper_1.SettlementMapper.toDomain(updatedSettlement);
        if (!domain) {
            throw new Error('Failed to update settlement status');
        }
        return domain;
    }
    async delete(id) {
        const settlement = await this.settlementModel.findById(id);
        if (!settlement) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
        if (settlement.status !== settlement_schema_1.SettlementStatus.PENDING) {
            throw new settlement_error_1.SettlementValidationError('Only pending settlements can be deleted');
        }
        const result = await this.settlementModel.deleteOne({ _id: id });
        if (result.deletedCount === 0) {
            throw new settlement_error_1.SettlementNotFoundError(id);
        }
    }
    async calculateBalances(groupId) {
        const group = await this.groupModel
            .findById(groupId)
            .populate('members.userId', 'name email');
        if (!group) {
            throw new Error(`Group with ID ${groupId} not found`);
        }
        const balances = [];
        for (const member of group.members) {
            balances.push({
                userId: member.userId['_id'].toString(),
                userName: member.userId['name'] || 'Unknown',
                amount: 0,
                paidAmount: 0,
                owedAmount: 0,
            });
        }
        const settlements = await this.settlementModel.find({
            groupId: new mongoose_2.Types.ObjectId(groupId),
            status: settlement_schema_1.SettlementStatus.COMPLETED,
        });
        for (const settlement of settlements) {
            const fromBalance = balances.find(b => b.userId === settlement.fromUserId.toString());
            if (fromBalance) {
                fromBalance.amount -= settlement.amount;
                fromBalance.owedAmount += settlement.amount;
            }
            const toBalance = balances.find(b => b.userId === settlement.toUserId.toString());
            if (toBalance) {
                toBalance.amount += settlement.amount;
                toBalance.paidAmount += settlement.amount;
            }
        }
        return balances.sort((a, b) => b.amount - a.amount);
    }
    async existsById(id) {
        const count = await this.settlementModel.countDocuments({ _id: id });
        return count > 0;
    }
    async findPendingSettlementsByUser(groupId, userId) {
        const userObjectId = new mongoose_2.Types.ObjectId(userId);
        const settlements = await this.settlementModel.find({
            groupId: new mongoose_2.Types.ObjectId(groupId),
            status: settlement_schema_1.SettlementStatus.PENDING,
            $or: [
                { fromUserId: userObjectId },
                { toUserId: userObjectId },
            ],
        })
            .populate('fromUserId', 'name email')
            .populate('toUserId', 'name email')
            .exec();
        return settlements
            .map(settlement => settlement_mapper_1.SettlementMapper.toDomain(settlement))
            .filter(Boolean);
    }
    validateSettlement(settlement) {
        if (settlement.amount <= 0) {
            throw new settlement_error_1.SettlementValidationError('Amount must be greater than 0');
        }
        if (settlement.fromUserId === settlement.toUserId) {
            throw new settlement_error_1.SettlementValidationError('Cannot settle with yourself');
        }
        if (settlement.status === settlement_schema_1.SettlementStatus.CANCELLED && settlement.isAutoGenerated) {
            throw new settlement_error_1.SettlementValidationError('Auto-generated settlements cannot be cancelled');
        }
    }
    validateStatusTransition(currentStatus, newStatus) {
        const validTransitions = {
            [settlement_schema_1.SettlementStatus.PENDING]: [settlement_schema_1.SettlementStatus.COMPLETED, settlement_schema_1.SettlementStatus.CANCELLED],
            [settlement_schema_1.SettlementStatus.COMPLETED]: [settlement_schema_1.SettlementStatus.CANCELLED],
            [settlement_schema_1.SettlementStatus.CANCELLED]: [],
        };
        if (!validTransitions[currentStatus]?.includes(newStatus)) {
            throw new settlement_error_1.SettlementValidationError(`Invalid status transition from ${currentStatus} to ${newStatus}`);
        }
    }
};
exports.SettlementRepository = SettlementRepository;
exports.SettlementRepository = SettlementRepository = __decorate([
    (0, common_1.Injectable)(),
    __param(0, (0, mongoose_1.InjectModel)(settlement_schema_1.Settlement.name)),
    __param(1, (0, mongoose_1.InjectModel)(group_schema_1.Group.name)),
    __metadata("design:paramtypes", [mongoose_2.Model,
        mongoose_2.Model])
], SettlementRepository);
//# sourceMappingURL=settlement.repository.js.map