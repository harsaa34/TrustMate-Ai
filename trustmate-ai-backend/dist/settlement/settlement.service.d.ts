import { Model } from 'mongoose';
import { SettlementDocument, SettlementStatus } from './settlement.schema';
import { GroupDocument } from '../group/group.schema';
import { CreateSettlementDto, UpdateSettlementStatusDto, SettlementResponseDto, BalanceResponseDto, SettlementOptimizationResultDto, SettlementStatisticsDto } from './settlement.dto';
export declare class SettlementService {
    private readonly settlementModel;
    private readonly groupModel;
    private readonly _logger;
    constructor(settlementModel: Model<SettlementDocument>, groupModel: Model<GroupDocument>);
    createSettlement(groupId: string, createDto: CreateSettlementDto, createdByUserId: string): Promise<SettlementResponseDto>;
    getGroupSettlements(groupId: string, filters?: {
        status?: SettlementStatus;
        fromDate?: Date;
        toDate?: Date;
        userId?: string;
        isAutoGenerated?: boolean;
    }): Promise<SettlementResponseDto[]>;
    getSettlementById(settlementId: string): Promise<SettlementResponseDto>;
    updateSettlementStatus(settlementId: string, updateDto: UpdateSettlementStatusDto): Promise<SettlementResponseDto>;
    deleteSettlement(settlementId: string): Promise<void>;
    calculateBalances(groupId: string): Promise<BalanceResponseDto[]>;
    optimizeSettlements(groupId: string): Promise<SettlementOptimizationResultDto[]>;
    createOptimizedSettlements(groupId: string, createdByUserId: string, maxSettlements?: number): Promise<SettlementResponseDto[]>;
    getSettlementStatistics(groupId: string): Promise<SettlementStatisticsDto>;
    private validateStatusTransition;
    private mapToResponseDto;
}
